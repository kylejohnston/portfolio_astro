---
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';

interface Props {
  entry: CollectionEntry<'projects'>;
  why?: string;
}

const { entry, why } = Astro.props;
---

<a class="project-card reveal" href={`/work/${entry.id}`}>
  <Image class="project-card__cover" src={entry.data.cover} alt={entry.data.coverAlt} quality="max" densities={[1, 2]} width={549} height={309} />
  <div class="project-card__body">
    <p class="project-card__summary">{entry.data.summary}</p>
    {why && <p class="project-card__why"><strong>Why:</strong> {why}</p>}
    <p class="project-card__title"><strong>Role:</strong> {entry.data.client}</p>
  </div>
</a>

<style>
  .project-card {
    display: block;
    corner-shape: superellipse(1.6);
    border-radius: 20px;
    padding: 8px;
    background: hsla(0, 24%, 100%, 1);
    -webkit-backdrop-filter: blur(8px);
    backdrop-filter: blur(8px);
    color: inherit;
    text-decoration: none;
    box-shadow:
    0px 0px 0px 1px rgba(35, 26, 26, 0.06),
    0px 1px 2px -1px rgba(35, 26, 26, 0.06),
    0px 2px 4px 0px rgba(35, 26, 26, 0.04);
    transition: box-shadow .25s ease;
  }
  .project-card:hover {
      box-shadow: 0 20px 25px -5px #0000001a,0 10px 10px -5px #0000000a;
  }
  .project-card__cover {
    display: block;
    width: 100%;
    height: auto; 
    aspect-ratio: 16 / 9;
    object-fit: cover;
    corner-shape: superellipse(1.6);
    border-radius: 12px;
    margin-bottom: 0;
    outline: 1px solid hsla(0, 15%, 12%, 0.05);
    outline-offset: -1px;
  }
  .project-card__body {
    padding: 6px 12px;
  }
  .project-card__title {
    font-size: var(--text-sm);
    line-height: var(--text-sm--lh);
    text-wrap: balance;
    margin-block: 4px;
    font-family: var(--font-text);
  }
  .project-card__summary,
  .project-card__why
  {
    text-wrap: balance;
    margin-block: 4px;
    font-family: var(--font-text);
  }
@media (min-width: 640px) {
  .project-card__body {
    padding: 8px 16px;
  }
  .project-card__summary,
  .project-card__why
  {
    text-wrap: balance;
    margin-block: 8px;
  }
}
</style>
