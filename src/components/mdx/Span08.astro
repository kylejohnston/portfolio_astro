---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

interface Props {
  src: ImageMetadata;
  alt: string;
}

const { src, alt } = Astro.props;
---

<figure class="col-start-3 col-span-8">
  <Image src={src} alt={alt} class="w-full h-auto my-3" />
</figure>
