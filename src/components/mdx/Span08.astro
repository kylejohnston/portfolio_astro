---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

interface Props {
  src: ImageMetadata;
  alt: string;
  rounded?: boolean;
  shadow?: boolean;
  border?: boolean;
}

const { src, alt, rounded = false, shadow = false, border = false } = Astro.props;
---

<figure class="col-span-12 sm:col-start-3 sm:col-span-8">
  <Image
    src={src}
    alt={alt}
    quality="max"
    densities={[1, 2]}
    class:list={[
      'w-full h-auto my-3',
      rounded && 'rounded',
      shadow && 'shadow-[0_0_16px_0_rgba(35,26,26,0.08)]',
      border && 'border border-[var(--color-border)]',
    ]}
  />
</figure>
